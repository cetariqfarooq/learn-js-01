let score = null
// console.log(typeof score);
let value_in_number = Number(score)
// console.log(typeof(value_in_number));

/*
"33" is converted into a Number
"33abc" is also converted into a Number but value is NaN
true returns 1 and false return 0
*/

let is_logged_in = 1
let bool_is_logged_in = Boolean(is_logged_in)
// console.log(bool_is_logged_in);

/*
1 is converted into True nd 0 is converted into false
empy string "" means false and "TF" means true
*/

//******************** Operations ************************//
let val = 3
let neg_val = - val
console.log(neg_val);
